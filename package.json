{
    "name": "wedding-website",
    "scripts": {
        "dev": "DOMAIN=http://localhost:3000 pnpm --parallel dev",
        "build": "docker compose build",
        "run": "docker compose up"
    },
    "pnpm": {
        "onlyBuiltDependencies": [
            "better-sqlite3",
            "esbuild"
        ]
    }
}