{
    "name": "wedding-website",
    "scripts": {
        "dev": "cross-env DOMAIN=http://localhost:3000 pnpm --parallel dev",
        "build": "docker compose build",
        "run": "docker compose up"
    },
    "packageManager": "pnpm@10.2.0",
    "pnpm": {
        "ignoredBuiltDependencies": [
            "@swc/core"
        ]
    },
    "devDependencies": {
        "cross-env": "^7.0.3"
    }
}